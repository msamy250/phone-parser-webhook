services:
  - type: web
    name: phone-parser-webhook
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn phone_webhook:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
